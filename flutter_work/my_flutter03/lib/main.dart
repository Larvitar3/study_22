
void main(){

  // 3 > 0011<<
  assert((3<<1) == 5);

  // Dart 에서 상수
  // const - comefile 시점에서 사용 , final( 런타임)
  // const myScore = 1000;
  // print(myScore);
  //
  // final mouth = 12;
  // final year = 2022;
  // print(year);

  // 초기화 할때 컴파일 시점에 초기화 되느냐 런타임 시점에 초기화 되느냐

  // 문자열 UTF -  16 사용

  var s1 = '싱글 쿼터즈 사용 가능';
  var s2 = '싱글 쿼터즈 사용 가능';

  print(s1.length);

}
